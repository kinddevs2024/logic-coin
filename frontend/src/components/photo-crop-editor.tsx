import type { PhotoCropEditorProps } from "./photo-crop-editor.types";

// On native, ImagePicker returns the image already cropped by its platform UI.
export function PhotoCropEditor(_props: PhotoCropEditorProps) { return null; }
